import {
    makeStyles
} from '@material-ui/core';

export const useStyles = makeStyles(theme => ({
    
    form: {
        width: '70%',
        display: 'flex',
        flexDirection: 'column',
        marginBottom:'10px'
    },
    inputs: {
        margin: 4,
        width: '100%',
    },
    
}));
